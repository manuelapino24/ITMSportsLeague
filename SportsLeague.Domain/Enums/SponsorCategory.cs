namespace SportsLeague.Domain.Enums;

public enum SponsorCategory
{
    Main = 0,    // main sponsor (Patrocinador principal)
    Gold = 1,    // gold (Oro)
    Silver = 2,  // silver (Plata) 
    Bronze = 3   // bronze(Bronce)
}